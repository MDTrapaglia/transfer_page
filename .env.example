# Dirección y puerto de escucha (el proxy de Nginx se conecta a este bind local).
FILEBROWSER_BIND_IP=127.0.0.1
FILEBROWSER_PORT=3050

# Carpeta local a exponer dentro del contenedor en /srv.
FILEBROWSER_ROOT=/home/mtrapaglia/Data/shared-folder
